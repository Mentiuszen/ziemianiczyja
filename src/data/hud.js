/** Presentation only. Never use these values in combat or AI. */
export const HUD=Object.freeze({range:60,minimapHz:30,maxDpr:2,maxContacts:128,maxMarkers:64,losPerFrame:4,losAge:.15,markerRange:60});
export const CONTACT_DURATION=Object.freeze({recruit:3,soldier:2,veteran:1});
