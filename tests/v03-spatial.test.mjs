import test from 'node:test';import assert from 'node:assert/strict';import {SpatialIndex} from '../src/world/spatial-index.js';import {rayBox,rng,norm} from '../src/core/math.js';
test('static grid ray candidates never omit a true slab hit including negative cells and vertical rays',()=>{const r=rng(3939),boxes=Array.from({length:150},(_,i)=>{const x=r()*120-60,z=r()*120-60;return{id:i,min:{x,y:-1,z},max:{x:x+r()*15+.1,y:4,z:z+r()*15+.1}};}),index=new SpatialIndex(boxes);for(let i=0;i<450;i++){const o={x:r()*180-90,y:r()*5,z:r()*180-90},d=i%10===0?{x:0,y:-1,z:0}:norm({x:r()-.5,y:(r()-.5)*.2,z:r()-.5}),candidates=new Set(index.segment(o,d,140));for(const b of boxes)if(rayBox(o,d,b,140)!==null)assert.ok(candidates.has(b),`miss ${b.id}`);}});
