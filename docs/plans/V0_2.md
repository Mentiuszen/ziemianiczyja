# Ziemia Niczyja v0.2 — wykonanie zatwierdzonego zakresu

Cel: naprawić wejście myszy, layout menu i niebo; poprawić żołnierzy, zmniejszyć koszty renderowania/symulacji i pokazać uczciwe pomiary FPS / CPU / GPU.
Baza: dostarczone archiwum v0.1.0, bez zmian repozytorium użytkownika, bez branchy, worktree i publikacji.
Nie dodajemy nowych misji ani migracji silnika. Zachowujemy format checkpointów v1 i przypisania klawiszy.

## 1. Diagnoza i regresje
- [x] Odtworzyć v0.1 w przeglądarce: rzeczywiste zdarzenia LPM, PPM, kombinacja obu, powrót z pauzy.
- [x] Zmierzyć rozmiar scroll viewportu menu; udokumentować niebo i koszty.
- [x] Dodać testy jednostkowe sekwencji Pointer Events przed naprawą.

## 2. Input i layout
Pliki: src/input/input.js, src/render/view.js, src/ui/style.css, tests/input.test.mjs, tools/browser_v02_regression.py.
- [x] Jeden właściciel wejścia gry; Pointer Events z maską buttons i kompatybilnym fallbackiem myszy.
- [x] Wyczyszczenie stanu po utracie fokusu, pauzie i wczytaniu. Zachowanie krótkiego kliknięcia.
- [x] Menu główne mieści się w viewportach desktop/laptop i małych rozmiarach CSS viewportu. Dekoracja mapy nie tworzy scrolla. Ustawienia przewijają wyłącznie własny panel.
- [x] npm test i sprawdzenie obu przycisków w prawdziwej przeglądarce.

## 3. Niebo
Pliki: src/render/sky.js, src/render/landscape.js.
- [x] Zastąpić cylinder i płaski dekiel pełną kopułą, bez zdegenerowanych UV na suficie.
- [x] Niebo pozostaje za terenem, przy kamerze, poza cieniami i pickingiem.
- [x] Sprawdzić horyzont, zenit, różne kierunki i ustawienia jakości; dolna półsfera sprawdzana geometrią, zasłonięta terenem w grze.

## 4. Pomiary i optymalizacja
Pliki: src/performance/*, src/app.js, src/render/view.js, src/ui/ui.js, src/save/store.js; bez zmiany kolizji.
- [x] Testy agregacji próbek, niedostępności GPU, disjoint i zwalniania query.
- [x] Asynchroniczne WebGL2 timer queries: ns na ms, bez gl.finish, bez udawania GPU = czas klatki.
- [x] CPU = praca aplikacji na głównym wątku, osobno symulacja i submission/render CPU; klatka obejmuje też oczekiwanie.
- [x] Ustawienia Pokaż FPS / CPU ms / GPU ms zapisane lokalnie i mały overlay niezależny od F3.
- [x] Optymalizacje na podstawie pomiarów: culling cieni/LOD, częstotliwość animacji, HUD bez zbędnych przebudów, bez zmiany liczby żołnierzy i zasad kolizji.
- [x] Porównanie bazowej i nowej wersji tym samym scenariuszem; jawna informacja o programowym GPU testowym.

## 5. Modele postaci
Pliki: tools/generate_characters.py, public/assets/models/{british,german}.glb, src/render/assets.js.
- [x] Autorskie szczegółowe mundury, sylwetki, twarze, oporządzenie i odmienne hełmy z odniesieniem do źródeł muzealnych.
- [x] Wspólny szkielet, teksturowana tkanina/skóra/metal, 3 rzeczywiste poziomy szczegółowości, niższy koszt odległych postaci.
- [x] Regenerowalny pipeline offline i rejestr zasobów; bez kopiowania modeli z gier lub muzealnych zdjęć jako tekstur.
- [x] Inspekcja modeli i animacji w rendererze, nie sama walidacja pliku GLB.

## 6. Dostawa
- [x] npm test, npm run check, npm run build, bot misji, testy browser input/layout/lifecycle.
- [x] Raport rzeczywistych wyników, changelog i znane ograniczenia.
- [x] ZIP projektu oraz ZIP dist; wszystkie pliki lokalne, ścieżki względne.


## Wynik odbioru

Zamknięto implementację v0.2 w dostarczonym zakresie, nie końcowy odbiór master promptu.
Źródła i dist przechodzą regresje w Chromium/SwiftShader. Firefox pozostaje niewykonany;
nie potwierdzono na nim pierwotnego błędu wejścia ani naprawy. Ustawienia mają implementację
trwałego zapisu, lecz testy tego środowiska potwierdzają tylko fallback na opaque originie.
Porównanie wydajności obejmuje także gorszy CPU startu; patrz TEST_REPORT.md i PERFORMANCE.md.
